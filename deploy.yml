name: CI
on:
  push:
    branches:
    - main

jobs:
  sync:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
    - uses: actions/setup-node@v1
    with:
      node-version: 12
    - run: npm ci
    - run: npm run build